# search_memory(query|filters)->ids
# stitch_episode(episode_id)->mp4_path
# tag_episode(episode_id, tags[])
# write_journal(session|episode, text)->md_path
# notify_user(text, paths=[]) (drops in reports/)